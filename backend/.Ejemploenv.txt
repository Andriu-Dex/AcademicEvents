# Base de datos PostgreSQL
DATABASE_URL=postgresql://postgres:tu_contraseña@localhost:5432/academicevents

# Servidor
PORT=3000
HOST=localhost
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:3000

# Seguridad
JWT_SECRET=clave_secreta_segura

# Email SMTP (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=correo@gmail.com
SMTP_PASS=contraseña_aplicacion

# Imgur (opcional)
IMGUR_CLIENT_ID=tu_imgur_client_id
IMGUR_CLIENT_SECRET=tu_imgur_client_secret

# Credenciales de Super Admin
SUPER_ADMIN_EMAIL=admin@uta.edu.ec
SUPER_ADMIN_PASSWORD=Admin12345
SUPER_ADMIN_CEDULA=9999999999

# Estados automáticos de eventos
EVENT_STATUS_CRON_ENABLED=true
EVENT_STATUS_CRON_SCHEDULE="*/5 * * * *"
EVENT_STATUS_TIMEZONE="America/Guayaquil"

# Firebase Admin SDK (backend)
FIREBASE_PROJECT_ID=tu_project_id_firebase
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Notas:
# 1) Descarga la clave privada JSON en Firebase Console -> Project settings -> Service accounts.
# 2) Guarda el archivo en backend/firebase-service-account.json.
# 3) Nunca subas ese JSON al repositorio.
# 4) Las variables VITE_FIREBASE_* se configuran solo en frontend/Ejemplo.env.txt.